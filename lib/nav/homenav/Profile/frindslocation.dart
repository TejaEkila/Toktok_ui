import 'package:flutter/material.dart';

class FriendsLocation extends StatelessWidget {
  const FriendsLocation({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.deepOrangeAccent,
    );
  }
}